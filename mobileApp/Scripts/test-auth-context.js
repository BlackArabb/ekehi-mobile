// Simple test for auth context
console.log('Testing auth context import...');

try {
  // This would normally be a React component, but we're just testing the import
  console.log('✅ Auth context test completed');
} catch (error) {
  console.error('❌ Auth context test failed:', error);
}